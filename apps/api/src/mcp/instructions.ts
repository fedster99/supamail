export const MCP_INSTRUCTIONS = `SupaMail provides read-only access to synced email and can prepare reply content. It cannot send, save drafts, move, delete, flag, or otherwise change mail.

The available tools are search_email, read_message, read_thread, list_folders, and draft_reply.

search_email returns results grouped by conversation by default, with 25 results by default and at most 100. A result's identity.id is its message_id. read_message accepts one message_id. It returns 4,096 body characters by default and accepts body_offset plus max_body_chars up to 32,768; body_total_chars and body_next_offset describe the cleaned body range. read_thread accepts one message_id, 1 to 10 message_ids, or an account-scoped conversation_id or provider thread_id. Each thread returns at most 20 messages by default and 100 when requested. Duplicate message_ids are collapsed in first-occurrence order; each distinct ID has its own result or error entry.

read_message and thread replies contain newly authored text by default. Recognized quoted reply tails and signatures are removed unless include_quoted is true. When no older messages were omitted, the oldest mirrored message keeps quoted content within the same 4,096-character body limit. body_truncated reports whether more cleaned text remains. list_folders returns folders containing mirrored messages, their total and unread counts, and account-wide totals. draft_reply returns prepared reply content but does not save or send it. Read results include sync_trust, which describes mirror completeness. Request errors include a code, message, and hint; batch thread errors use the same fields in the affected thread entry.`;
