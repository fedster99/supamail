export const MCP_INSTRUCTIONS = `SupaMail provides read-only access to synced email and can prepare reply content. It cannot send, save drafts, move, delete, flag, or otherwise change mail.

The available tools are search_email, read_message, read_thread, list_folders, and draft_reply.

search_email returns results grouped by conversation by default, with 25 results by default and at most 100. A result's identity.id is its message_id. read_message accepts one message_id and returns its full available cleaned body by default. body_offset and max_body_chars can return a specific range without a product character ceiling; body_total_chars and body_next_offset describe the range. body_content_status and body_omissions explicitly state whether available source text is absent and why, including source_truncated when sync stored only part of the original source. read_thread accepts one message_id, 1 to 10 message_ids, or an account-scoped conversation_id or provider thread_id. Each thread returns at most 20 messages by default and 100 when requested. thread_content_status, thread_omissions, and omitted_message_count explicitly state when older messages are absent. Duplicate message_ids are collapsed in first-occurrence order; each distinct ID has its own result or error entry.

read_message and read_thread return the full available cleaned body for each message. Replies contain newly authored text by default. Recognized quoted reply tails and signatures are removed unless include_quoted is true. When no older messages were omitted, the oldest mirrored message keeps quoted content. list_folders returns folders containing mirrored messages, their total and unread counts, and account-wide totals. draft_reply returns prepared reply content but does not save or send it. Read results include sync_trust, which describes mirror completeness. Request errors include a code, message, and hint; batch thread errors use the same fields in the affected thread entry.`;
